后端（FastAPI，端口 8000）

cd agoracompaignmanager/backend
cp .env.example .env
# 在 .env 中填入 ANTHROPIC_API_KEY（必填）
pip install -r requirements.txt
uvicorn app.main:app --reload
前端（React + Vite，端口 5173）

cd agoracompaignmanager/frontend
npm install
npm run dev
可选：导入测试数据

cd agoracompaignmanager/backend
python seed.py



Shengwang QA account
15213366726   
Lht123456@